@tailwind base; @tailwind components; @tailwind utilities;
:root { color-scheme: light; }
.card { @apply rounded-2xl bg-white shadow-soft p-6 hover:shadow-lg transition; }
.h1 { @apply text-3xl font-semibold; }
.sub { @apply text-sm text-slate-500; }
.nav { @apply sticky top-0 z-10 bg-white/80 backdrop-blur border-b border-slate-200; }
.btn { @apply inline-flex items-center gap-2 rounded-xl px-4 py-2 bg-brand-600 text-white hover:bg-brand-700; }